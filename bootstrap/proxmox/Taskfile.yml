# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      - aws sts get-caller-identity || aws configure sso
      - infisical run --path=/proxmox -- terraform {{.CLI_ARGS}}
    silent: false
