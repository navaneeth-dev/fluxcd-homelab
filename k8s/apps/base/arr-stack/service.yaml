apiVersion: v1
kind: Service
metadata:
  name: arr-stack
  namespace: homelab
spec:
  type: ClusterIP
  ports:
    - name: sabnzbd
      port: 8080
      targetPort: 8080
  selector:
    app: arr-stack
